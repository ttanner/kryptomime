language: python
before_install:
  - sudo apt-get update
  - sudo apt-get install gnupg
python:
  - "2.7"
  - "3.3"
  - "3.4"
install:
  - pip install gnupg
  - pip install psutil
  - pip install pytest
  - pip install pytest-pythonpath
  - pip install coverage
  - python setup.py install
script:
  - coverage run --source kryptomime -m py.test tests
  - coverage report
